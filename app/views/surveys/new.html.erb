<h1>Create a New Survey</h1>

<%= form_for @survey do |survey_fields| %>
  <%= survey_fields.label :survey_title %>
  <%= survey_fields.text_field :name %>

  <%= survey_fields.label :description %>
  <%= survey_fields.text_area :description %>

  <%= survey_fields.submit :create %>

<% end %>
